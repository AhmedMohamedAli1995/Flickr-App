# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PhotoPreviewer' do
  # Uncomment the next line if you're using Swift or would like to use dynamic frameworks
  # use_frameworks!

  # Pods for PhotoPreviewer
	pod 'AFNetworking', '~> 3.0'
	pod 'SDWebImage', '~> 4.0'
	pod 'Realm'

  target 'PhotoPreviewerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PhotoPreviewerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
